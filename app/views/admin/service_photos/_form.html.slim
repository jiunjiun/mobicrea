== simple_form_for [:admin, service, service_photo], url: admin_service_photo_path(service, service_photo), method: :put, html: { autocomplete: 'off' } do |f|
  .panel.colourable
    /! Default panel contents
    .panel-body
      .text-center
        - if service_photo.image.present?
          == image_tag service_photo.image, class: 'img-responsive img-thumbnail'
        hr/
      == f.input :title
      == f.input :subtitle
      hr/
      == f.input :title_eng
      == f.input :subtitle_eng
      hr/
      == f.input :url, as: :url
      == f.input :new_page

  .panel
    .panel-body
      == f.button :submit, class: 'btn-primary btn-3d'
      | &nbsp;&nbsp;
      == link_to t('helpers.back'), admin_service_path(service), class: 'btn btn-default btn-3d'
